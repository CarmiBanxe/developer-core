{
  "mcpServers": {
    "context7": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp@latest"],
      "description": "Актуальная документация — добавь 'use context7' к промпту"
    },
    "figma": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@figma/mcp-server"],
      "env": {
        "FIGMA_ACCESS_TOKEN": "${FIGMA_ACCESS_TOKEN}"
      },
      "description": "Figma семантическое чтение дизайна (токены, компоненты, Auto Layout)"
    },
    "storybook": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@chromatic/mcp-server"],
      "description": "Storybook компоненты как живой контекст в Claude Code"
    }
  }
}
